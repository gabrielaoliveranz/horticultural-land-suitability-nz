# Terroir — development-only dependencies
# Not needed to run the ingestion pipeline or the dashboard; install
# alongside requirements.txt only if you're doing this kind of dev work.

-r requirements.txt

# Used for the one-off deuteranopia/protanopia simulation that verified
# the suitability-map colour scheme (see dashboard/pages/2_Suitability_Map.py
# and README.md, "Accessibility-verified visualisation"). Not imported by
# any script or dashboard page at runtime, so it doesn't belong in
# requirements.txt — it's a verification tool, not a runtime dependency.
colorspacious==1.1.2
