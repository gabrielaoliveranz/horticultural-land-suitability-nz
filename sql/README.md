# SQL

Schema definitions and queries: exploratory queries and the subzone
scoring queries (joins between soil, climate, and cadastral tables;
aggregations; rankings; outlier detection).

## Status

To be populated. No schema or queries have been written yet. When they
are, every file will follow the SQL header standard in
`docs/conventions.md`.
