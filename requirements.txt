# Terroir — dependencies
# Pinned to the versions installed in the working environment (`pip freeze`).
# Every package here is imported directly somewhere in src/ or dashboard/ —
# numpy, shapely, pandas, pydeck, and altair were previously left as
# unpinned transitive dependencies of geopandas/streamlit, which is fragile.

requests==2.34.2
python-dotenv==1.2.2
geopandas==1.1.4
numpy==2.5.1
shapely==2.1.2
pandas==3.0.5

# Dashboard (Fase 5) — multipage nav uses st.Page/st.navigation, built
# into Streamlit core since 1.36; no separate nav package needed.
streamlit==1.60.0
pydeck==0.9.3
altair==6.2.2
