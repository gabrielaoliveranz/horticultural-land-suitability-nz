# Docs

Index of project documentation and decisions for Terroir.

## Status

- `conventions.md` — complete. File header standard, naming rules, and
  structural discipline for the whole project.
- `data_sources.md` — complete (Fase 0). LINZ and S-map access confirmed
  via WFS; NIWA evaluated and dropped in favour of Open-Meteo.
- `business_questions.md` — complete (Fase 1). Five questions closed.
- `methodology.md` — dashboard tool decision (Streamlit), the
  waterlogging drainage-only proxy decision, and the parcel selection
  (area threshold + LCDB as attribute) decision recorded. Scoring model
  section started — the null-handling rule (exclude, don't impute) is
  fixed; weightings still to be populated (Fase 3).
- `attributions.md` — six sources logged (LINZ, four S-map layers,
  Open-Meteo).
